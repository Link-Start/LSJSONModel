# Uncomment the next line to define a global platform for your project
platform :ios, '13.0'

target 'LSJSONModel_Example' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for LSJSONModel_Example
  pod 'LSJSONModel', :path => '../'

  target 'LSJSONModel_Tests' do
    inherit! :search_paths
    # Pods for testing
  end

end